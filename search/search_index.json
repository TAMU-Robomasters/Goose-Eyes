{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Goose-Eyes","text":"<p>Autonomy code base for the 26-27 season. The runtime is a dora-rs dataflow: each node is its own process (Supports Python,C,C++,Rust), wired together by a YAML graph. The main platform is the Jetson Orin; see Setup for getting an environment on either it or a laptop.</p> <p>This site is the hub: it documents how the system is structured and what each component's contract is (inputs, outputs, message types, rates). Generated API reference sits underneath in API Reference.</p>"},{"location":"#system-map","title":"System map","text":"<p>Everything that runs today is one dora dataflow, the camera pub/sub demo. ROS 2 Jazzy is installed in the environment (via RoboStack) for the navigation work to come, but nothing in this repo uses it yet.</p> <pre><code>flowchart LR\n  subgraph dora[\"dora dataflow (goose-eyes/)\"]\n    direction LR\n    camera[\"camera_pub\"] -- image --&gt; display[\"camera_sub\"]\n  end\n  subgraph ros[\"ROS 2 Jazzy (in the env, unused so far)\"]\n    nav[\"Nav2\"]\n  end\n  dora -. \"planned bridge\" .-&gt; ros</code></pre>"},{"location":"#where-to-look","title":"Where to look","text":"Setup install, how pixi works, the <code>jetson</code> platform, adding a dev platform dora running the demo, architecture (generated graph), per-node contracts API Reference Python module docs from docstrings; Rust / C++ slots reserved Updating the docs the three <code>pixi run docs*</code> tasks and the docs-update workflow"},{"location":"contributing-docs/","title":"Updating the docs","text":"<p>The site is MkDocs + Material, configured in <code>mkdocs.yml</code>, with the pages in <code>docs/</code>. The toolchain is its own pixi environment (<code>docs</code>), so it never touches the runtime env; the tasks below find that environment on their own.</p>"},{"location":"contributing-docs/#the-three-commands","title":"The three commands","text":"<pre><code>pixi run docs-serve    # live preview at http://127.0.0.1:8000 \u2014 edit a page, the browser reloads\npixi run docs          # full build into site/ with --strict: broken links and nav mistakes fail it\npixi run docs-gen      # only regenerate docs/generated/ (dora graph \u2192 Mermaid); the other two run it first\n</code></pre> <p><code>site/</code> is build output and gitignored. <code>docs/generated/</code> is checked in \u2014 commit it when a dataflow changes, so a <code>--strict</code> build is reproducible without dora installed.</p>"},{"location":"contributing-docs/#when-you-change","title":"When you change\u2026","text":"you changed update a dataflow YAML nothing by hand \u2014 <code>pixi run docs-gen</code> regenerates the diagram. If a node was added/removed, add/remove its page under <code>docs/dora/nodes/</code> and its line in the <code>nav:</code> of <code>mkdocs.yml</code>. a node's inputs, outputs, metadata or rate its page in <code>docs/dora/nodes/</code> \u2014 the tables are the contract; keep them true. a docstring nothing \u2014 the API pages are rendered from the source at build time. <code>pixi.toml</code> platforms or targets <code>docs/setup/platforms.md</code> / <code>docs/setup/adding-a-platform.md</code>. a page's location or name the <code>nav:</code> in <code>mkdocs.yml</code>, and any links to it (<code>--strict</code> will tell you). <p>Conventions: mark anything you don't know as <code>TODO</code> rather than guessing, and never write a \"Why\" for a decision you didn't make \u2014 a <code>TODO: rationale</code> is more useful than an invented one. Docstrings are Google style. Repo files that aren't pages get linked with a full GitHub URL, since MkDocs only resolves links inside <code>docs/</code>.</p>"},{"location":"api/","title":"API Reference","text":"<p>Under Construction</p>"},{"location":"api/python/SUMMARY/","title":"SUMMARY","text":"<ul> <li>camera_pub</li> <li>camera_sub</li> </ul>"},{"location":"api/python/camera_pub/","title":"<code>camera_pub</code>","text":""},{"location":"api/python/camera_sub/","title":"<code>camera_sub</code>","text":""},{"location":"dora/","title":"dora","text":"<p>How the dora side is laid out and how to run the demo. The generated graph of the dataflow is on Architecture; each node's contract is under Nodes.</p> <p>Everything lives in <code>goose-eyes/</code>: <code>dataflows/</code> holds the dataflow graphs (<code>*.yml</code>) and <code>nodes/</code> the scripts they run (a node's <code>path:</code> is resolved relative to the dataflow file, hence <code>../nodes/\u2026</code>). Logs for each run land in <code>out/&lt;run-id&gt;/</code> next to the dataflow.</p>"},{"location":"dora/#camera-pubsub-demo","title":"Camera pub/sub demo","text":"<p>A minimal dora-rs dataflow: one node captures webcam frames, one node displays them, wired together over shared memory. It shows how nodes, inputs and outputs are connected.</p> <pre><code>camera_pub.py \u2500\u2500image\u2500\u2500\u25b6 camera_sub.py\n</code></pre> File Role <code>dataflows/dataflow.yml</code> The graph: node ids, which script each runs, and how outputs feed inputs <code>nodes/camera_pub.py</code> Reads frames with OpenCV, flattens them to a <code>pyarrow</code> array and sends them on <code>image</code> (frame shape goes in metadata) <code>nodes/camera_sub.py</code> Receives <code>image</code>, reshapes it back with the metadata, shows it in a window <p>Run it from the repo root. <code>dora</code> only exists inside the pixi environment, so either open a shell in it once or prefix the command with <code>pixi run</code>:</p> <pre><code>pixi shell                                    # drops you into the env; `exit` to leave\ndora run goose-eyes/dataflows/dataflow.yml\n</code></pre> <pre><code>pixi run dora run goose-eyes/dataflows/dataflow.yml    # same thing, one command\n</code></pre> <p>Press <code>q</code> in the camera capture node window to stop.</p> <p><code>nodes/camera_pub.py</code> opens camera index <code>0</code> (<code>cv2.VideoCapture(0)</code>), usually the built-in webcam. If you get <code>error: camera didn't open</code>, change the index to match your machine.</p>"},{"location":"dora/#rerun","title":"rerun","text":"<p>The environment includes the rerun SDK for visualisation, but no dataflow logs to it yet. On the Jetson it is installed outside the lock \u2014 run <code>pixi run install-rerun</code> once (see Platforms); on a laptop it comes with <code>pixi install</code>.</p>"},{"location":"dora/architecture/","title":"Architecture","text":"<p>One diagram per dataflow in <code>goose-eyes/dataflows/</code>, generated by <code>dora graph &lt;yml&gt; --mermaid</code> into <code>docs/generated/</code> (<code>pixi run docs-gen</code>; the <code>docs</code> and <code>docs-serve</code> tasks run it first). Don't edit the diagrams by hand \u2014 change the YAML and regenerate. Node ids in the diagram are the <code>id:</code> fields in the YAML; the script each id runs is listed in the table below.</p> <p>The one edge today carries a flattened BGR frame as a <code>pyarrow</code> <code>uint8</code> array with <code>shape</code> and <code>time</code> in the metadata (see camera_pub).</p>"},{"location":"dora/architecture/#dataflowyml-camera-pubsub-demo","title":"<code>dataflow.yml</code> \u2014 camera pub/sub demo","text":"<p><code>dora run goose-eyes/dataflows/dataflow.yml</code> (inside <code>pixi shell</code>, or prefixed with <code>pixi run</code>). One capture node, one display node reading its output over shared memory.</p> <pre><code>flowchart TB\n  camera[\\\"**camera**\"/]\n  displayer[/\"**displayer**\"\\]\n  camera -- image --&gt; displayer\n</code></pre> id script <code>camera</code> <code>nodes/camera_pub.py</code> <code>displayer</code> <code>nodes/camera_sub.py</code>"},{"location":"dora/nodes/camera_pub/","title":"<code>camera_pub</code>","text":"<p><code>goose-eyes/nodes/camera_pub.py</code> \u2014 the source node: reads a webcam with OpenCV, resizes to 640\u00d7480, publishes each frame and shows it in a preview window. <code>q</code> in that window stops it. Runs as <code>camera</code> in <code>dataflow.yml</code>.</p>"},{"location":"dora/nodes/camera_pub/#inputs","title":"Inputs","text":"<p>None.</p>"},{"location":"dora/nodes/camera_pub/#outputs","title":"Outputs","text":"output type metadata rate <code>image</code> <code>pa.array(uint8)</code>, flattened BGR frame (<code>480*640*3</code>) <code>shape: (480, 640, 3)</code>, <code>time: float</code> (<code>time.time()</code> at capture) camera-limited (typically 30 fps). TODO: measure on the Jetson <p>Camera index is hard-coded (<code>cv2.VideoCapture(0)</code>, usually the built-in webcam); change it if you get <code>error: camera didn't open</code>.</p>"},{"location":"dora/nodes/camera_sub/","title":"<code>camera_sub</code>","text":"<p><code>goose-eyes/nodes/camera_sub.py</code> \u2014 sink that reshapes the frame from its <code>shape</code> metadata and shows it with <code>cv2.imshow</code> (window second). Exits on the dataflow's <code>STOP</code> event. Runs as <code>displayer</code> in <code>dataflow.yml</code>.</p>"},{"location":"dora/nodes/camera_sub/#inputs","title":"Inputs","text":"input type metadata used rate <code>image</code> <code>pa.array(uint8)</code>, flattened BGR frame <code>shape</code> follows camera_pub"},{"location":"dora/nodes/camera_sub/#outputs","title":"Outputs","text":"<p>None.</p>"},{"location":"setup/","title":"Setup","text":"<p>The whole environment \u2014 Python, OpenCV, ROS 2 Jazzy, dora, rerun \u2014 comes from one pixi manifest, <code>pixi.toml</code>.</p> <pre><code>curl -fsSL https://pixi.sh/install.sh | sh     # once\npixi install                                   # from the repo root\npixi run install-rerun                         # Jetson only \u2014 rerun lives outside the lock there\n</code></pre>"},{"location":"setup/#pixi-in-one-minute","title":"pixi in one minute","text":"<p>pixi is a package manager that installs everything (conda-forge packages and PyPI packages) into a project-local environment under <code>.pixi/</code>. Nothing goes on your system. What you need to know:</p> <ul> <li><code>pixi.toml</code> lists the dependencies; <code>pixi.lock</code> records the exact versions that were solved   and is checked in, so everyone gets the same environment. <code>pixi install</code> reads the lock.   (manifest \u00b7   lock file)</li> <li> <p>Running things. The environment isn't on your <code>PATH</code> by default. Either enter it with   <code>pixi shell</code> and work normally (<code>exit</code> to leave),   or prefix a single command with <code>pixi run</code>:</p> <p><pre><code>pixi shell\ndora run goose-eyes/dataflows/dataflow.yml\n</code></pre> <pre><code>pixi run dora run goose-eyes/dataflows/dataflow.yml\n</code></pre></p> </li> <li> <p>Tasks are named shortcuts defined in <code>pixi.toml</code> (<code>pixi task list</code>). This repo keeps them for   tooling only \u2014 <code>install-rerun</code>, the <code>docs*</code> tasks \u2014 not for running dataflows.   (tasks)</p> </li> <li>Adding a dependency: <code>pixi add &lt;pkg&gt;</code>   (<code>pixi add --pypi &lt;pkg&gt;</code> for PyPI). It re-solves the lock for every platform and fails right there if   the package doesn't exist for one of them.</li> <li>Platforms and targets. The manifest is solved for the Jetson and for macOS laptops at once;   platform-specific dependencies live in <code>[target.&lt;platform&gt;.*]</code> tables. See   Platforms and Adding a platform.</li> <li>ROS 2 Jazzy comes from the RoboStack conda channel as ordinary   dependencies \u2014 no <code>source /opt/ros/...</code>, it's just there inside the environment. Nothing in   <code>goose-eyes/</code> uses it yet.</li> <li>The docs toolchain is a separate pixi environment (<code>docs</code>) so the runtime env stays small; the   <code>pixi run docs*</code> tasks pick it automatically. (environments,   Updating the docs)</li> </ul>"},{"location":"setup/adding-a-platform/","title":"Adding a development platform","text":"<p>The Jetson is the main target and <code>osx-arm64</code> is the only dev platform so far. Adding another (say <code>linux-64</code> for an x86 laptop) is a <code>pixi.toml</code> change; pixi's multi-platform docs cover the mechanics.</p> <ol> <li>Declare it \u2014 add the platform string to <code>platforms = [...]</code> in <code>pixi.toml</code>    (<code>\"linux-64\"</code>, <code>\"win-64\"</code>, <code>\"osx-64\"</code>). Plain strings are fine for laptops; only the Jetson needs    the named-platform form, because it pins a glibc.</li> <li> <p>Add substitutes for what the Jetson gets from JetPack. Copy the <code>osx-arm64</code> target tables:</p> <pre><code>[target.linux-64.dependencies]\nopencv = \"&gt;=4.13.0,&lt;5\"\n\n[target.linux-64.pypi-dependencies]\nrerun-sdk = \"&gt;=0.38.1, &lt;0.39\"\n</code></pre> <p>Keep the <code>rerun-sdk</code> pin equal to <code>RERUN_VERSION</code> in <code>utils/install_jetson_rerun.sh</code>. 3. Re-solve and install \u2014 <code>pixi lock</code> solves    the lock for every platform (this is where you find out if a dependency doesn't exist for the new    one; ROS 2 from RoboStack is the likely culprit), then <code>pixi install</code>. Commit the whole <code>pixi.lock</code>. 4. Smoke test:</p> <pre><code>pixi run python -c \"import cv2, dora, rerun, rclpy; print(cv2.__version__, rerun.__version__)\"\npixi run dora run goose-eyes/dataflows/dataflow.yml    # needs a webcam\n</code></pre> </li> </ol> <p>Things to know:</p> <ul> <li><code>[dependencies]</code> is for packages that exist on every declared platform; anything else goes in a   <code>[target.&lt;platform&gt;.*]</code> table, or the whole solve fails.</li> <li>A <code>[target.&lt;platform&gt;.activation]</code> <code>scripts</code> list replaces the workspace-level one rather than   adding to it. Only the Jetson has activation scripts today.</li> <li>Don't declare <code>cuda</code> on any platform (see Platforms).</li> <li>Laptops are partial support by design: anything hardware-bound (NVIDIA GStreamer, TensorRT) won't run   there. Note the gap in Platforms rather than working around it.</li> <li>Windows is untested; the activation scripts are bash and cameras are <code>/dev/video*</code>. WSL2 is the   lower-effort route.</li> </ul>"},{"location":"setup/platforms/","title":"Platforms","text":"<p><code>pixi.toml</code> declares two platforms, and one <code>pixi.lock</code> is solved for both. <code>jetson</code> is the main target \u2014 the robot runs there. <code>osx-arm64</code> (dev laptops) is partial support: same dora / ROS 2 / rerun stack, but conda-forge OpenCV instead of JetPack's and no CUDA / TensorRT, so most things can be developed there but not everything will run.</p> <code>jetson</code> (main) <code>osx-arm64</code> (dev, partial) Hardware / OS AGX Orin / Orin NX on JetPack 7: Ubuntu 24.04, CUDA 13.2 Apple Silicon Mac OpenCV JetPack's 4.8 (GStreamer with the NVIDIA plugins) conda-forge 4.13 TensorRT JetPack's 10.16 \u2014 numpy 1.26 (<code>&lt;2</code>, forced by JetPack's cv2) 2.x rerun-sdk <code>pixi run install-rerun</code>, outside the lock in the lock <p>The commands are the same on both: <code>pixi install</code>, <code>pixi shell</code> / <code>pixi run \u2026</code>.</p>"},{"location":"setup/platforms/#how-its-wired","title":"How it's wired","text":"<p>The <code>jetson</code> platform. It's a named platform entry in <code>pixi.toml</code> (<code>{ name = \"jetson\", platform = \"linux-aarch64\", glibc = \"2.39\" }</code>): it solves for <code>linux-aarch64</code> but pins the glibc a JetPack 7 board has, so <code>pixi install</code> on an older JetPack (Ubuntu 22.04) refuses instead of producing a half-working env. Jetson-only dependencies and activation live under <code>[target.jetson.*]</code>, laptop substitutes under <code>[target.osx-arm64.*]</code> (targets, virtual packages).</p> <p>JetPack's OpenCV / TensorRT. The Jetson target declares no conda OpenCV. Instead an activation script, <code>utils/jetson_activate.sh</code>, runs whenever the env is entered and puts JetPack's <code>cv2</code> / <code>tensorrt</code> on <code>PYTHONPATH</code>, the env's <code>lib/</code> first on <code>LD_LIBRARY_PATH</code> (avoids a glib clash between conda and NVIDIA libraries), and the NVIDIA GStreamer plugins on <code>GST_PLUGIN_PATH</code>. The script's comments explain each line. Check it on a board:</p> <pre><code>pixi run python -c \"import cv2, tensorrt; print(cv2.__version__, cv2.__file__, tensorrt.__version__)\"\n# 4.8.0 /usr/lib/python3.12/dist-packages/cv2/__init__.py 10.16.2.10\n</code></pre> <p>rerun on the Jetson is installed outside the lock. JetPack's <code>cv2</code> needs numpy 1.x, every recent <code>rerun-sdk</code> declares numpy \u2265 2, and pixi can't override that metadata \u2014 so the two can't be in one solve. rerun 0.38.1 actually runs fine on numpy 1.26, so <code>utils/install_jetson_rerun.sh</code> installs the wheel with <code>--no-deps</code> (<code>pixi run install-rerun</code>; no-op once installed, <code>--force</code> to reinstall). Its real runtime deps are in the Jetson solve. Keep the version in that script equal to the <code>rerun-sdk</code> pin in <code>pixi.toml</code> so both platforms speak the same rerun wire format.</p> <p>No CUDA declared. Adding <code>cuda = \"13.2\"</code> to the <code>jetson</code> entry would make conda-forge prefer CUDA builds of packages, which drag in conda's own CUDA runtime instead of JetPack's. GPU Python packages on the Jetson should come from NVIDIA's JetPack wheels.</p>"},{"location":"setup/platforms/#gotchas","title":"Gotchas","text":"<ul> <li>On the Jetson, <code>pixi run</code> / <code>pixi shell</code> put conda's <code>lib/</code> first on <code>LD_LIBRARY_PATH</code>, so system   tools run from inside the env (<code>nvcc</code>, <code>tegrastats</code>, \u2026) see conda's libraries too. Harmless so far; run   a system tool outside pixi if it misbehaves.</li> </ul>"}]}